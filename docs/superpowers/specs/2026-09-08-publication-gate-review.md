# D1 착수 전 계약 재검토

기준: 2026-09-08, HEAD `1b4dc05`. 설계 모델에서 코드와 완료 기록을 대조했다. 이번 문서는 구현 완료 보고가 아니다.

## 확인 결과

실원본 반복 일치는 유지되는 근거지만, 잘못된 입력을 거부하는 계약의 모든 조건을 입증하지 않는다. 공개 전 검증 계획 Task 1~4의 체크를 전체 완료 근거로 사용하지 않는다.

| 영역 | 현재 구현과 계획의 차이 | 구현 인수 조건 |
| --- | --- | --- |
| 월 관측 불변식 | 하루에 25개 관측도 parseMonthResult가 허용한다 | 관측 수가 양끝 날짜 사이 일수×24를 초과하면 거부 |
| 오류 진단 | counts 합계 안전 정수, 코드별 샘플 수 상한, 일반 객체 조건 미검증. toMonthResult의 valid 경로가 진단을 빈 객체로 대체하며 invalid 경로는 최종 parse를 생략 | 진단을 보존하고 입력/출력 모두 동일 검증, 건수보다 많은 샘플과 합계 overflow 거부 |
| 원본 추적 | metadata 누락 시 hashJson(null)을 원본 해시로 사용. run.contract와 monthly.input의 전체 의미 일치 미검증 | 실제 원본 해시·정규화 계약 필수, 계약 expectedSha256과 원본 참조 일치, 입력 전체 일치 검증 |
| 산출물 판독 | 직접 readMonthlyOutput은 failure 표식 충돌을 검사하지 않음. 실패 판독은 성공 파일 공존·원본/처리 계약을 충분히 검사하지 않음 | 모든 공개 reader가 같은 표식 정책 사용, 실패 후보는 검증된 MonthResult 전체 보존 |
| 비교 계약 | fallbackReason은 previous_year_unavailable로 고정. compareMonths는 changes를 직접 검증하지 않음. 후보 기간 오류는 일반 Error여서 CLI 코드 2 | 실제 전년 실패 사유 보존, 직접 호출도 변경 목록 검증, 기간 설정 오류는 코드 1 |
| 파일 제한 | stat 이후 readFile로 전체 파일을 할당. 다른 limits는 파일 읽기 후 검증 | 모든 제한 먼저 검증, 한 핸들에서 최대 제한+1 바이트까지만 읽고 초과 거부. 해시와 파싱은 같은 Buffer |
| 출력 경로 | workRoot 옵션 생략 시 writer 검사 우회. root 자체 링크와 root 내부 링크 경유 허용 가능 | writer의 workRoot 필수화, 경로 구성요소의 링크/재분석 지점 거부, 미존재 root의 실제 경로 재구성 검증 |
| CLI 기본값 | parseNamedArgs의 required 목록에 work-root가 있어 기본값 도달 불가 | CLI는 프로젝트 data/work를 기본으로 사용. 테스트는 임시 프로젝트 data/work로 실행하며 기본값 경로 자체 검증 |
| 회귀 테스트 | 0/00 테스트의 first도 row helper 때문에 00. 역순·중복으로 누락을 가린 사례 없음 | 실제 0과 00 입력, 역순 동일 결과, 동일 행 수라도 중복/누락 거부 |

읽기 전용 재현: parseNamedArgs에서 work-root 생략 시 `Missing argument: --work-root`; 기존 월 JSON의 한 동을 20260701 하루·25관측·719누락으로 바꾸면 parseMonthResult가 수락했다. 원본 파일은 수정하지 않았다. 전체 테스트는 이번 검토에서 다시 실행하지 않았다.

## 다음 구현 범위

기존 공개 전 검증 계획을 재개하되 위 표의 누락 조건을 우선한다. 월 불변식 → 원본/산출물 계약 → 비교 계약 → 파일/경로 경계 순으로 각각 거부 사례와 정상 왕복을 검증한다. 형식이나 본문이 바뀌면 새 work 경로에서 실원본 인수를 재실행한다. 기존 산출물은 보존한다.

## D1 후속 설계에 유지할 결정

- 기존 D1 개요 설계의 불변 버전·building 스냅샷·조건부 공개 포인터 방향을 유지한다. 선행 계약 보완 후 상세 SQL/구현 계획을 작성한다.
- comparison_sets 부모가 현재·전년·전월의 검증된 입력 참조와 비교 정책 버전을 소유한다. invalid 후보는 월 관측 행으로 위장하지 않고 실패 산출물 참조로 보존한다.
- public_channels의 generation 조건으로 공개 경합을 판정한다. 복구 대상은 validated 또는 이전 published 스냅샷이며 실제 공개 여부는 포인터로 판단한다.
- 명칭 목록 근거와 통계 경계 근거를 구분한다. method unverified인 실제 값은 기존 공개 정책에 따라 숨긴다.

사용자 합의에 따라 다음 코드 수정은 구현용 모델 전환 후 수행한다. 이번 검토에서 앱 코드·D1 리소스·원격 저장소는 변경하지 않았다.

## 구현 진행 기록

2026-09-08 구현용 모델에서 월 관측 범위, 진단 객체/합계/샘플, source·contract 참조, fallbackReason과 직접 변경 목록 검증을 보완했다. 파일 제한 읽기, writer workRoot 강제와 링크 검증, 회귀 입력 보강은 다음 구현 단위다.
